#all classes will extend
class command():


	def __init__ (self, name):
		self.name = name

	def sendCommand():
		#TODO: call some function send() which will return info based on what is sent. At the moment, most likely just a string
		return self.name 